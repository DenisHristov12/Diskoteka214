import styled from 'styled-components';

export const ButtonMenu = styled.div`
  border: none;
  outline: none;

  color: var(--color-brand-500);

  height: 3rem;
  width: 3rem;

  & svg {
    height: 3rem;
    width: 3rem;
  }
`;
